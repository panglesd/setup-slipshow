name: Setup Slipshow
description: Setup Slipshow, adds it to the PATH.
branding:
  icon: 'monitor'
  color: 'yellow'

inputs:
  version:
    description: Which version to install
    required: false
    default: latest

runs:
  using: node24
  main: dist/index.js
